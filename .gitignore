# segredos / config local (webhook do Slack)
config.local.json
__pycache__/

# CSVs de trabalho na RAIZ (coleta bruta avulsa) — não versionar.
# Os snapshots semanais em registros/<AAAA-MM-DD>/ SÃO versionados (drill-down).
/*.csv
